using System;
using System.IO;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
            StreamWriter fileStream = null;
        //    HeadWriter(StreamWriter fileStream);
        //    EndWriter(StreamWriter fileStream);
        //    WriteTitle(StreamWriter fileStream, string str);
       //     WriteAuthor(StreamWriter fileStream, string str);
       //     WriteCountPages(StreamWriter fileStream, string str);
      //      WriteWetherRead(StreamWriter fileStream, string str);
        }
    }
}
